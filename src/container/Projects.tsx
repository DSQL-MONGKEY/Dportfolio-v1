import { styles } from "@constants/styles"

const Projects = () => {
   return (
      <section className={`${styles.padding} ${styles.paddingY}`}>
         Projects
      </section>
   )
}

export default Projects